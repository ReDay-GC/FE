# Re:Day Project Guide

## 프로젝트 개요
Re:Day는 사용자의 하루 기록을 모아 하나의 **기억(memory)** 으로 재구성하는 Android 기반 기억 기록 앱이다.

사용자는 사진, 텍스트 메모, 짧은 음성 기록을 남길 수 있고,
앱은 각 기록의 시간, 위치, 내용 정보를 바탕으로 하루 단위의 **memory card(기억 카드)** 를 생성한다.

이 앱은 단순 메모 앱이 아니라,
여러 개의 **기록 조각(fragment)** 을 수집하고 이를 AI가 분석하여 **기억 단위로 재구성하는 것**이 핵심인 서비스이다.

---

## 핵심 개념

### 1. 기록 조각 (Record Fragment / Record Piece)
사용자가 직접 추가하는 가장 작은 입력 단위이다.

예:
- 사진 기록
- 텍스트 메모 기록
- 음성 기록

각 기록 조각에는 필요에 따라 다음 정보가 연결될 수 있다.
- 시간
- 위치
- 메모/텍스트
- 미디어 정보(사진/음성)

### 2. 기억 (Memory / Day Memory)
하루 동안 쌓인 여러 기록 조각을 AI가 분석하여 재구성한 **하루 단위 기억 단위**이다.

기억은 다음과 같은 정보를 가질 수 있다.
- 기억 제목
- 기억 내용 초안
- 기억 요약
- 태그
- 대표 조각(썸네일/대표 미리보기)
- 포함된 기록 조각 목록

### 3. 대표 조각 (Thumbnail / Representative Fragment)
하나의 기억은 여러 기록 조각으로 이루어지며,
사용자는 그중 하나를 **대표 조각**으로 선택할 수 있다.

대표 조각은 다음 화면에서 대표 미리보기 역할을 한다.
- 홈의 최근 기억
- 보관함
- 기타 기억 리스트 화면

대표 조각은 반드시 사진일 필요는 없으며 다음 모두 가능하다.
- 사진 조각
- 텍스트 조각
- 음성 조각

---

## 주요 사용자 흐름
1. 사용자가 사진 / 텍스트 / 음성 기록 조각을 남긴다.
2. 각 기록 조각에는 시간 정보와 위치 정보가 연결된다.
3. AI가 기록 조각들을 분석하여 하루 단위 기억을 생성한다.
4. 사용자는 AI가 생성한 기억 결과를 수정할 수 있다.
5. 사용자는 대표 조각을 선택하고 기억을 저장한다.
6. 저장된 기억은 홈, 캘린더, 지도, 보관함, 분석 화면에서 활용된다.

---

## 프로젝트 핵심 기능 맥락

### 기록 조각 입력
사용자는 다음 형태의 기록 조각을 생성할 수 있다.
- 사진 기록
- 텍스트 메모 기록
- 짧은 음성 기록

### 시간 정보
- 사진은 EXIF 시간 정보를 우선 활용할 수 있다.
- 텍스트와 음성은 생성/입력 시각을 기본으로 사용한다.
- 필요 시 사용자가 시간을 수정할 수 있다.
- 시간 정보는 필요 시 source 개념(EXIF / CURRENT / MANUAL)을 고려할 수 있다.

### 위치 정보
- 사진은 EXIF 위치 정보를 우선 활용할 수 있다.
- 사진 업로드 시 EXIF 위치 정보를 읽을 수 있으면 위치 입력 칸을 자동으로 채우는 방향을 우선 고려한다.
- EXIF 위치가 없거나 부정확할 경우 GPS 또는 수동 입력을 사용한다.
- 텍스트/음성 기록도 현재 위치 또는 수동 위치 입력을 사용할 수 있다.
- 위치 기능은 **실시간 추적이 아니라 기록 추가 시점의 1회 현재 위치 조회**를 기준으로 해석한다.
- 위치 정보는 필요 시 source 개념(EXIF / CURRENT / MANUAL)을 고려할 수 있다.

### 사진 기록
- 사진 업로드 시 EXIF의 시간/위치 정보를 우선 활용한다.
- EXIF 위치를 읽을 수 있으면 위치 입력 칸에 자동 반영하는 방향을 우선 고려한다.
- EXIF를 가져오지 못하거나 위치가 부정확하면 사용자가 수동 수정할 수 있어야 한다.
- 사진 조각에는 관련 메모를 함께 작성할 수 있어야 한다.

### 음성 기록
- 음성 기록은 짧은 음성 입력을 전제로 한다.
- 현재 설계 기준으로 음성 기록은 **최대 10초**를 기준으로 고려한다.
- 음성 기록은 STT를 통해 텍스트 초안으로 변환할 수 있어야 한다.
- STT 결과는 최종 확정본이 아니라 **사용자가 수정 가능한 메모 초안**이어야 한다.

### AI 기반 기억 생성
AI는 기록 조각들을 분석하여 다음과 같은 결과를 생성할 수 있다.
- 기억 제목
- 기억 내용 초안
- 기억 요약 정보
- 태그
- 하루 단위 memory card

AI가 생성한 결과는 최종본이 아니라 **초안**이며,
사용자가 직접 수정 가능해야 한다.

### 기억 탐색 및 활용
생성/저장된 기억은 다음 화면 또는 기능에서 활용된다.
- 홈
- 캘린더
- 지도
- 보관함
- 분석

### 지도 기능
- 지도 기능은 Google Maps 기반 활용을 우선 고려한다.
- 기억 또는 기억 조각의 위치 정보를 지도 위 마커로 표시하는 방향을 고려한다.
- 위치는 실시간 추적이 아니라 저장된 기록/기억을 탐색하기 위한 정보로 활용한다.

### 분석 기능
분석 기능은 다음과 같은 방향을 가진다.
- 기억 통계
- 자주 만난 사람
- 자주 방문한 장소
- 활동 유형 분석
- 월간 기억 인사이트

---

## 기술 스택
현재 확정된 기술 스택:
- Android Studio
- Kotlin
- Android 앱 프로젝트
- Gradle 기반 빌드

확정되지 않은 기술 스택은 임의로 추측하지 않는다.
예를 들어 UI 방식(XML/View, Compose 등), DI, DB, 네트워크 세부 구현 등은
**현재 프로젝트 구조를 먼저 확인한 뒤 그에 맞는 방식으로 제안한다.**

---

## 개발 원칙
- 기존 구조를 최대한 보존한다.
- 큰 구조 변경은 사용자가 명시적으로 요청했을 때만 제안한다.
- 한 번에 많은 것을 바꾸지 말고 단계적으로 진행한다.
- 요구사항이 애매하면 중요한 기능을 임의로 상상하지 않는다.
- 사용자가 말하지 않은 기능이나 표현을 임의로 추가하지 않는다.
- 구현 전에 현재 구조를 먼저 분석한다.
- 수정 전에 어떤 파일이 관련되는지 먼저 설명한다.
- 가능하면 최소 수정으로 해결한다.
- 무리한 리팩토링보다 안정적인 변경을 우선한다.

---

## 작업 방식
기본 작업 순서는 항상 아래를 따른다.

1. 현재 구조 분석
2. 관련 파일 확인
3. 구현/수정 계획 간단 설명
4. 필요한 경우 사용자 확인 또는 명확한 지시 후 수정
5. 변경 후 무엇이 바뀌었는지 요약

사용자가 **한 단계씩** 진행하길 원하면,
절대 여러 단계를 한 번에 진행하지 말고 현재 단계만 안내한다.

사용자가 기존 코드를 먼저 보여주지 않은 상황에서
기존 구조가 필요한 작업이라면 임의 추측보다 먼저 현재 파일 구조를 확인한다.

---

## 커뮤니케이션 규칙
- 기본 응답은 한국어로 한다.
- 설명은 실용적이고 명확하게 한다.
- 장황한 이론 설명보다 실제 구현에 도움이 되는 방향을 우선한다.
- 사용자의 요구사항과 추측을 구분해서 설명한다.
- 확정되지 않은 구조는 확정된 것처럼 말하지 않는다.
- "지금은 분석만", "지금은 수정 금지", "새 파일로만", "기존 구조 유지" 같은 조건을 엄격히 따른다.

---

## 아키텍처 선호
다음 구조 원칙을 우선 고려한다.

- 서버 응답 모델(Response/DTO)과 UI 모델은 분리한다.
- DTO를 화면에서 직접 사용하지 않는다.
- DTO → UI Model 변환은 mapper를 사용한다.
- 더미데이터는 UI 화면 클래스에 직접 하드코딩하지 않는다.
- 더미데이터는 repository / datasource 계층에서 공급한다.
- 화면 로직과 데이터 로직은 분리한다.
- 구조 제안 시 유지보수성과 확장성을 함께 고려한다.

선호 디렉터리 구조 예시:
- fragments 또는 ui
- adapters
- models
- network
- repository
- datasource
- mapper
- utils
- viewmodel

단, 실제 프로젝트 구조가 다르면
무조건 새 구조로 갈아엎지 말고 기존 구조와 조화롭게 적용한다.

---

## UI 구현 원칙
- UI는 먼저 구조를 분석한 뒤 구현한다.
- 스크린샷/디자인 시안 기반 요청이 오면 다음 순서를 따른다:
  1. 화면 구성 분석
  2. UI 요소 분해
  3. 화면/컴포넌트 구조 제안
  4. 코드 초안 작성
- 기존 큰 파일을 바로 덮어쓰지 않는다.
- 가능하면 새 파일 초안을 먼저 제안한다.
- 사용자가 "적용 말고 테스트만" 원하면 절대 기존 파일을 수정하지 않는다.
- UI 구현 시 디자인만 맞추는 것에 그치지 말고, 이후 데이터 연결을 고려한 구조를 제안한다.

---

## 데이터 및 더미데이터 원칙
백엔드 및 AI 연동 전에는 더미데이터를 사용할 수 있다.
단, 더미데이터도 실제 API 구조를 최대한 반영해서 설계한다.

규칙:
- 화면 클래스 내부에 임의 하드코딩 금지
- repository 또는 datasource에서 제공
- Response/DTO와 UI 모델은 분리
- mapper를 통해 UI 모델로 변환
- 나중에 실서버로 바꿀 때 변경 범위가 작도록 설계
- 더미데이터는 임의 값의 모음이 아니라 **예상 API 응답 구조를 최대한 반영한 샘플 데이터**여야 한다
- 화면에서는 DTO를 직접 바인딩하지 않고 UI Model을 사용한다

---

## AI 기능 관련 구현 원칙
이 프로젝트에서 AI는 부가 기능이 아니라 핵심 기능이다.

AI 관련 구현 또는 설계 시 다음을 고려한다:
- 입력은 기록 조각(fragment) 단위다.
- 출력은 기억(memory) 단위다.
- AI 결과는 자동 생성되지만 사용자가 수정할 수 있어야 한다.
- AI 결과를 그대로 확정 데이터처럼 취급하지 않는다.
- 제목, 요약, 태그, 기억 내용은 AI 생성 후보 또는 초안으로 다룬다.
- AI 기반 기능을 설계할 때는 입력 데이터(사진/텍스트/음성/시간/위치)의 연결성을 고려한다.

---

## 기능 해석 기준
Re:Day의 기능을 해석할 때 다음 관점을 유지한다.

- 홈: 오늘의 기억과 최근 기억을 보여주는 진입 화면
- 캘린더: 날짜별 기억 및 생성 상태를 탐색하는 화면
- 지도: 장소 기반으로 기억을 탐색하는 화면
- 보관함: 누적된 기억을 목록/검색/월별로 조회하는 영역
- 분석: 사람/장소/활동/통계/월간 인사이트를 보여주는 영역
- 설정: 알림, 버전, 약관, 개인정보 관련 정보 관리 영역
- 알림: 생성 상태, 리마인드, 서비스 알림 등을 다루는 영역

---

## 편집 안전 규칙
- 파괴적인 명령은 명시적으로 요청받지 않으면 실행하지 않는다.
- 여러 파일을 대량 삭제/이동/이름변경하지 않는다.
- 필요 이상의 새 라이브러리를 추가하지 않는다.
- 대규모 구조 개편은 먼저 계획을 제시한다.
- 사용자가 "수정하지 말고"라고 하면 절대 파일을 수정하지 않는다.
- 사용자가 "코드만 보여줘"라고 하면 실제 적용 대신 초안만 제공한다.

---

## 구현 시 우선 확인할 항목
구현 또는 수정 전에 우선 확인할 것:
1. 현재 프로젝트 파일 구조
2. 관련 Fragment / Activity / Adapter / ViewModel 존재 여부
3. 기존 모델 / DTO / mapper 존재 여부
4. 더미데이터 공급 방식(repository/datasource) 존재 여부
5. 현재 화면이 실제 데이터 기반인지, 더미데이터 기반인지

없는 구조를 임의로 있다고 가정하지 않는다.

---

## Claude가 우선 기억해야 할 점
1. Re:Day는 AI 기반 기억 생성 앱이다.
2. 입력은 기록 조각이고 출력은 기억이다.
3. 기록 조각과 기억은 분리된 개념이다.
4. 하루 기억은 여러 기록 조각으로 구성된다.
5. 대표 조각은 사진이 아닐 수도 있으며 사용자가 직접 선택한다.
6. DTO/Response와 UI 모델은 분리한다.
7. 더미데이터는 repository/datasource 계층에서 관리한다.
8. 기존 구조를 함부로 크게 바꾸면 안 된다.
9. 사용자는 단계별 진행을 선호한다.
10. 수정 전에 분석과 계획 설명이 필요하다.
11. 사용자가 말하지 않은 기능은 임의로 추가하지 않는다.
