<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ReDay" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- BottomNavigationView 활성 인디케이터 비활성화 -->
        <item name="colorSecondaryContainer">@android:color/transparent</item>
        <item name="colorOnSecondaryContainer">@color/main_200</item>
    </style>

    <style name="Theme.ReDay" parent="Base.Theme.ReDay" />

    <style name="Theme.ReDay.Splash" parent="Base.Theme.ReDay">
        <item name="android:windowBackground">@color/main_100</item>
    </style>

    <style name="Theme.ReDay.Logo" parent="Base.Theme.ReDay">
    </style>
</resources>